{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <h1>{{ product.name }}</h1>
        <p>{{ product.description }}</p>
        <p>{{ product.price }}</p>
    </div>
{% endblock %}
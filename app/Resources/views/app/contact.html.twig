{% extends 'base.html.twig' %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <div class="card-body">
                {{ form_start(form) }}
                    {{ form_row(form) }}
                    <button class="btn btn-success" type="submit">Send</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
{% endblock %}
